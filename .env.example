# ============================================
# Forimage 图床配置文件
# 复制此文件为 .env 并修改配置值
# ============================================

# ==================== 必需配置 ====================

# 应用密钥（用于加密会话，请修改为随机字符串）
# 生成方法: python -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET_KEY=change-this-to-a-random-secret-key

# JWT密钥（用于用户认证，请修改为随机字符串）
# 生成方法: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-this-to-a-random-jwt-key

# 数据库连接（MySQL）
DATABASE_URL=mysql+aiomysql://username:password@localhost:3306/imgbed

# ==================== 可选配置 ====================

# 运行环境 (development/production)
APP_ENV=production

# 上传文件存储路径（相对于 backend 目录）
UPLOAD_PATH=./uploads

# ==================== Redis配置（可选） ====================
# 备份功能的定时任务需要 Redis

REDIS_ENABLED=false
# REDIS_URL=redis://localhost:6379/0

# ==================== 备份加密密钥（可选） ====================
# 用于加密备份节点的凭证信息（AES-256-GCM）
# 生成方法: python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
# BACKUP_ENCRYPTION_KEY=your_base64_encoded_32_byte_key

# ============================================
# 以下配置已迁移到后台管理界面
# 请在 管理后台 -> 系统设置 中配置
# ============================================
# - 站点信息（名称、标题、描述、Logo等）
# - 上传设置（大小限制、格式限制、压缩质量）
# - 存储设置（本地/OSS/COS/S3）
# - 邮件设置（SMTP服务器配置）
# - 安全设置（频率限制、自动封禁）
# - 内容审核（阿里云/腾讯云审核服务）
